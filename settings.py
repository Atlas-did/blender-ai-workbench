"""
settings.py — 默认配置与常量定义
=================================
所有可配置项的默认值集中在这里。
偏好设置面板 (preferences.py) 会读取/覆盖这些默认值。
"""

# ---------------------------------------------------------------------------
# 插件标识
# ---------------------------------------------------------------------------
ADDON_NAME = "aiwork"
ADDON_LABEL = "AIWork"

# ---------------------------------------------------------------------------
# LLM / API 默认值
# ---------------------------------------------------------------------------
# Kimi (月之暗面 Moonshot) — OpenAI 兼容 API
DEFAULT_API_ENDPOINT = "https://api.moonshot.cn/v1"
DEFAULT_API_KEY = ""  # 在 Blender 偏好设置中填入你的 API Key
DEFAULT_MODEL_NAME = "moonshot-v1-auto"  # 自动选择最优模型，131K 上下文
DEFAULT_MAX_TOKENS = 4096
DEFAULT_TEMPERATURE = 1.0  # Kimi 等模型要求 temperature=1
DEFAULT_REQUEST_TIMEOUT = 120  # 秒，Kimi 有时响应较慢

# ---------------------------------------------------------------------------
# MCP (Model Context Protocol) 默认值
# ---------------------------------------------------------------------------
DEFAULT_MCP_ENABLED = False
DEFAULT_MCP_SERVER_ADDRESS = "http://localhost:8080"

# ---------------------------------------------------------------------------
# 上下文采集
# ---------------------------------------------------------------------------
DEFAULT_CONTEXT_MAX_OBJECTS = 50       # 单次最多上报的对象数
DEFAULT_CONTEXT_INCLUDE_WORLD = True   # 是否包含世界/环境信息
DEFAULT_CONTEXT_INCLUDE_RENDER = True  # 是否包含渲染设置
DEFAULT_CONTEXT_INCLUDE_FILEPATH = True

# ---------------------------------------------------------------------------
# UI 常量
# ---------------------------------------------------------------------------
PANEL_CATEGORY = "AIWork"
CHAT_INPUT_MAX_LENGTH = 4000
MESSAGE_PREVIEW_LENGTH = 80
LOG_MAX_LINES = 500
CONTEXT_REFRESH_INTERVAL = 2.0  # 秒，自动刷新上下文间隔（0 = 关闭）

# ---------------------------------------------------------------------------
# 工具执行
# ---------------------------------------------------------------------------
TOOL_EXEC_TIMEOUT = 30  # 秒，单个工具最长执行时间
MAX_TOOL_CHAIN_DEPTH = 10  # 单次对话最大工具链深度

# ---------------------------------------------------------------------------
# 存储路径
# ---------------------------------------------------------------------------
# 会话和配置保存在 Blender 扩展目录下的 aiwork/ 子目录
# 具体路径由 storage.py 在运行时解析
STORAGE_DIR_NAME = "aiwork"
SESSIONS_FILE = "sessions.json"
SETTINGS_FILE = "settings.json"
AUDIT_LOG_FILE = "audit.jsonl"

# ---------------------------------------------------------------------------
# 安全 / 风险等级
# ---------------------------------------------------------------------------
# 工具风险分三级，对应不同的确认策略
RISK_LOW = "low"         # 纯读取，无需确认
RISK_MEDIUM = "medium"   # 修改场景，需要确认（可批量）
RISK_HIGH = "high"       # 执行代码 / 文件操作，必须逐个确认

# 高风险工具需要用户输入 YES 文本确认
HIGH_RISK_CONFIRM_TEXT = "YES"
